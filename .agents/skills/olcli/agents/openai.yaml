interface:
  display_name: "Olcli"
  short_description: "Repo-specific patterns and workflows for olcli"
  default_prompt: "Use the olcli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true